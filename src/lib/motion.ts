// One easing curve, one duration scale, used by every entry/exit animation
// on the site (see globals.css's --ease-site for the CSS-side equivalent).
export const EASE = [0.16, 1, 0.3, 1] as const;
